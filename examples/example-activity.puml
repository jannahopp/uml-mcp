@startuml
start
:Receive order;
:Validate order;
if (Valid?) then (yes)
  :Process payment;
  if (Payment OK?) then (yes)
    :Ship order;
    stop
  else (no)
    :Notify customer;
    stop
  endif
else (no)
  :Reject order;
  stop
endif
@enduml